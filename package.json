{
  "name": "wp-to-emdash-migration-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.8.0",
  "scripts": {
    "build": "corepack pnpm -r build",
    "typecheck": "corepack pnpm -r typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "node -e \"import('node:fs/promises').then(fs => Promise.all(['packages/shared-types/dist','packages/migration-core/dist','packages/migration-cli/dist','packages/test-fixtures/dist'].map(path => fs.rm(path, { recursive: true, force: true }))))\""
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "fast-xml-parser": "^5.5.9",
    "zod": "^4.3.6"
  }
}
